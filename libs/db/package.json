{
  "name": "@vishwakarma-k-c/db",
  "private": true,
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "push": "drizzle-kit push --force",
    "studio": "drizzle-kit studio",
    "seed": "tsx --env-file=../../.env src/seed.ts",
    "test": "dotenv -e ../../.env -- vitest run"
  },
  "dependencies": {
    "@vishwakarma-k-c/shared": "workspace:*",
    "drizzle-orm": "^0.40.0",
    "postgres": "^3.4.5",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.5",
    "drizzle-zod": "^0.7.0"
  }
}
